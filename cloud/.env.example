# jka-cloud config. Copy to the VPS as /opt/jka/cloud.env (referenced by docker-compose.prod.yml's
# env_file). NEVER commit the filled-in version — it holds OAuth secrets and S3 keys.

# Public origin the browser hits. OAuth redirect URIs are derived as
# <BASE_URL>/api/auth/<provider>/callback — register EXACTLY that at each provider.
BASE_URL=https://jka.virtastic.app

# Session signing key (HS256). Generate: openssl rand -hex 32
JWT_SECRET=

# --- Storage. TWO modes:
#   Local (default): leave S3_* blank. Game data + saves persist to DATA_DIR (the /data volume in
#     compose) and the backend serves them itself. No object store, no CORS - just a disk. Simplest.
#   S3: fill S3_* below. The browser transfers DIRECTLY to S3 via presigned URLs (game data never
#     proxies through the backend). The bucket needs CORS allowing BASE_URL for GET/PUT/DELETE.
DATA_DIR=/data

# --- Abuse limits (all enforced server-side; defaults shown). Sized for a real a Jedi Academy install install
# (~1.5 GB, ~180 files) with headroom. Lower them if you host for a small group.
# MAX_FILE_BYTES=1073741824      # 1 GiB - one data file (assets1.pk3 is ~652 MB and assets0.pk3 ~561 MB)
# MAX_SAVE_BYTES=67108864        # 64 MB  - one savegame
# MAX_USER_BYTES=6442450944      # 6 GiB  - per account, data + saves
# MAX_USER_FILES=4000            # per account
# MAX_TOTAL_BYTES=107374182400   # 100 GB - whole install (local storage mode)

# Endpoint/region/bucket are per-deployment infrastructure - fill them in on the server, not here.
S3_ENDPOINT=
S3_REGION=
S3_BUCKET=
S3_ACCESS_KEY=
S3_SECRET_KEY=
# S3_FORCE_PATH_STYLE=1   # set for MinIO / path-style endpoints
# Root prefix inside the bucket. Production leaves this EMPTY; every non-production instance must
# set its own (e.g. dev/) so a dev login - especially with DEV_AUTH - can never read prod lockers.
# S3_PREFIX=dev/
# Run cloud/s3-setup.sh once per bucket to apply CORS (needed for direct browser uploads).

# --- OAuth apps (register each; only the ones you fill in show as live in the launcher).
DISCORD_CLIENT_ID=
DISCORD_CLIENT_SECRET=
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
MICROSOFT_CLIENT_ID=
MICROSOFT_CLIENT_SECRET=

# DEV_AUTH=1   # dev/test ONLY: enables /api/auth/dev/login (no real OAuth). Never in prod.

